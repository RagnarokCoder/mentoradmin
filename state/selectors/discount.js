export const selectDiscountStatus = (state) => state.discount.status;
export const selectDiscounts = (state) => state.discount.discounts;
export const selectDiscount = (state) => state.discount.discount;
export const selectSize = (state) => state.discount.size;
export const selectPage = (state) => state.discount.page;
