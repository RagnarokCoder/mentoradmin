export const selectTeamStatus = (state) => state.team.status;
export const selectTeams = (state) => state.team.teams;
export const selectTeam = (state) => state.team.team;
export const selectSize = (state) => state.team.size;
export const selectPage = (state) => state.team.page;
